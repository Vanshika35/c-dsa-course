#include<iostream>
using namespace std;

int main(){
    /*int a = 4;
    int b = 6;
    cout << " a & b equals " << (a&b) << endl;
    cout << " a | b equals " << (a|b) << endl;
    cout << " ~a equals " << (~a) << endl;
    cout << " ~b equals " << (~b) << endl;
    cout << " a ^ b equals " << (a^b) << endl;
    cout << " 17 >> 1 :" << (17>>1) << endl;
    cout << " 17 >> 2 :" << (17 >> 2) << endl;
    cout << " 19 << 1 :" << (19 << 1) << endl;
    cout << "21 << 2 :" << (21 << 2) << endl;
    // post increment operator->i++, preincrement->++i, post decrement->i--, pre decrement-> --i
    int i = 7;
    cout << (++i) << endl; // 8
    cout << (i++) << endl; // 8, i=9
    cout << (i--) << endl; // 9, i=8
    cout << (--i) << endl; // 7 ,i=7
    int a, b = 1;
    a = 10;
    if (++a){
        cout << b;
    }else{
        cout << ++b;
    }

    int a = 1;
    int b = 2;
    if(a-- > 0 && ++b > 2){
        cout << "Stage1 - Inside If ";
    }else{
        cout <<"Stage2 - Inside else ";
    }
    cout << a << " " << b << endl; // a=0, b=3
    int number = 3;
    cout << (25 * (++number));
    int a = 1;
    int b = a++;
    int c = ++a;
    cout << b << endl;//1
    cout << c;//3*/
    //for loop
    // int n;
    // cout << "enter the value of n : " << endl;
    // cin >> n;
    // cout << "printing count from 1 to n : " << endl;
    // for(int i = 1; i<=n;i++){
    //     cout << i << endl;
    // }
    
    /*int i = 1;
    for(; ; ){
        if(i<=n){
            cout << i << endl;
        }
        else{
            break;
        }
        i++;
    }
    for(int a = 0,b = 1, c = 2; a>=0 && b>=1 && c>=2; a--, b--, c--){
        cout << a << " " << b << " " << c << endl;
    }
    // calculate sum from 1 to n
    int n;
    cin >> n;
    int sum = 0;
    for(int i = 1; i<= n; i++){
        sum += i;
    }
    cout << sum;
    // fibonacci series
    int n;
    cout << "enter the value of n : ";
    cin >> n;
    int a = 0;
    int b = 1;
    cout << a << " " << b <<" ";
    for(int i = 1; i<= n; i++){
        int nextNumber = a+b;
        cout << nextNumber << " ";
        a = b;
        b = nextNumber;
    }
    int n;
    cout << "enter the value of n : ";
    cin >> n;

    bool isPrime = 1;
    for(int i = 2; i<n; i++){
        if(n%i == 0){
            //cout << "not prime";
            isPrime = 0;
            break;
        }
    }
    if(isPrime == 0){
        cout << "not a prime number" << endl;
    }else{
        cout << "is a prime number" << endl;
    }
    for(int i = 0; i < 5; i++){
        cout << "Hi" << endl;
        cout << " Hey " << endl;
        continue;


        cout << "Reply toh karde " << endl;
    }
    for(int i = 0; i<= 5; i++){
        cout << i << " ";
        i++;
    }
    // infinite loop
    for(int i = 0; i<=5; i--){
        cout << i << " ";
        i++;
    }
    for(int i = 0; i<= 15; i+=2){
        cout << i << " ";
        if(i&1)
            continue;
        i++;
    } 
    for(int i = 0; i<5; i++){
        for(int j = i; j<=5; j++){
            cout << i << " " << j << endl;
        }
    }
    for(int i = 0; i<5; i++){
        for(int j = i; j<=5; j++){
            if(i+j == 10){
                break;
            }
            cout << i << " " << j << endl;
        }
    }*/
}